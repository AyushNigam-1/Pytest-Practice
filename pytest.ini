[pytest]
markers =
    unit: Fast, isolated local tests that do not hit the network or DB.
    llm: Slow tests that make actual API calls to Gemini or other LLMs.
    integration: Tests that require a running database or external service.
